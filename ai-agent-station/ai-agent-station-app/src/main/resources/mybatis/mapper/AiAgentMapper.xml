<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiao.std.ai.agent.infrastructure.dao.IAiAgentDao">
    <insert id="insert" parameterType="aiAgent" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_agent (
            agent_id, agent_name, description, channel, status, create_time, update_time
        ) VALUES (
                     #{agentId}, #{agentName}, #{description}, #{channel}, #{status}, #{createTime}, #{updateTime}
                 )
    </insert>


    <update id="updateById" parameterType="aiAgent">
        UPDATE ai_agent SET
                            agent_id = #{agentId},
                            agent_name = #{agentName},
                            description = #{description},
                            channel = #{channel},
                            status = #{status},
                            update_time = #{updateTime}
        WHERE id = #{id}
    </update>

    <update id="updateByAgentId" parameterType="aiAgent">
        UPDATE ai_agent SET
                            agent_name = #{agentName},
                            description = #{description},
                            channel = #{channel},
                            status = #{status},
                            update_time = #{updateTime}
        WHERE agent_id = #{agentId}
    </update>

    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM ai_agent WHERE id = #{id}
    </delete>

    <delete id="deleteByAgentId" parameterType="java.lang.String">
        DELETE FROM ai_agent WHERE agent_id = #{agentId}
    </delete>

    <select id="queryById" parameterType="java.lang.Long" resultType="aiAgent">
        SELECT id, agent_id, agent_name, description, channel, status, create_time, update_time
        FROM ai_agent
        WHERE id = #{id}
    </select>

    <select id="queryByAgentId" parameterType="java.lang.String" resultType="aiAgent">
        SELECT id, agent_id, agent_name, description, channel, status, create_time, update_time
        FROM ai_agent
        WHERE agent_id = #{agentId}
    </select>

    <select id="queryEnabledAgents" resultType="aiAgent">
        SELECT id, agent_id, agent_name, description, channel, status, create_time, update_time
        FROM ai_agent
        WHERE status = 1
        ORDER BY create_time DESC
    </select>

    <select id="queryByChannel" parameterType="java.lang.String" resultType="aiAgent">
        SELECT id, agent_id, agent_name, description, channel, status, create_time, update_time
        FROM ai_agent
        WHERE channel = #{channel}
        ORDER BY create_time DESC
    </select>

    <select id="queryAll" resultType="aiAgent">
        SELECT id, agent_id, agent_name, description, channel, status, create_time, update_time
        FROM ai_agent
        ORDER BY create_time DESC
    </select>
</mapper>

